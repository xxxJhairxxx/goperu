---

import '@fontsource-variable/rubik';
import Container from '../globals/Container.astro';
//interface Props {menu: string;}

//const {menu} = Astro.props;
---
<Container>
<nav class="Navbar">
    <div class="logo">
        <img class="image" src="https://adelanteperu.com/wp-content/uploads/2023/07/logo-adp.svg" alt="">
    </div>
    <ul class="menu">
        <a href="/"><li>HOME</li></a>
        <a href="/acerca-de-julio-tovar"><li>ACERCA DE JULIO TOVAR</li></a>
        <a><li>EQUIPO</li></a>
        <a><li>IDEOLOGÍA</li></a>
        <a><li>NUESTRAS IDEAS</li></a>
        <a><li>SÚMATE</li></a>
        <a><li>CONTÁCTANOS</li></a>
    </ul>
</nav>
</Container>

<style>
	.Navbar {
		list-style: none;
		padding: calc(0.1rem * var(--scale));
        padding-top: calc(5rem * var(--scale));
        display: flex;
        font-size: calc(1.2rem * var(--scale));
        justify-content: space-between;
    }
    .menu{
        display: flex;
        flex-direction: row;
        list-style: none;
        color: white;
        font-weight: 500;
        height: fit-content;
         width: calc(87rem * var(--scale));
        padding: 0 calc(2.5rem * var(--scale));
        
        
        font-family: 'Rubik Variable', sans-serif;

    }
    .logo{
        width: calc(18rem * var(--scale));
        height: calc(18rem * var(--scale));
        margin-right: calc(10rem * var(--scale));
    }
	.image{
        width: 100%;
        height: 100%;
    }

    a{
        @apply flex items-center;
        background-color: #222222;
        padding: calc(1.4rem * var(--scale)) calc(1.8rem * var(--scale));
    cursor: pointer;
       height: calc(4rem * var(--scale));
    }

    a:hover{
    @apply bg-black ;
    }
</style>
